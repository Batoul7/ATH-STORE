# Online Shop

React + Vite online shopping project converted from TypeScript/TSX to JavaScript/JSX.

## Available Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

## Notes

- Source files now use `.js` and `.jsx`.
- TypeScript config files were removed.
- The production build was tested with `npm run build`.
# ATH-STORE
